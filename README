chrome extension for netflix request

Mac OS
=====
1. enable experiments API:
Go to "chrome://flags", enable "Experimental Extension APIs"

2. restart chrome with specific parameters (close all existing Chrome instances first):
"/Applications/Google\ Chrome.app/Contents/MacOS/Google\ Chrome --enable-experimental-extension-apis"

3. download the extension in chrome, and it will automatically be installed:
http://www.stanford.edu/~huangty/netflix_chrome_ext.crx

Ubuntu
======
1. Same as MAC OS
2. close all existing chrome instances, and run "/opt/google/chrome/google-chrome --enable-experimental-extension-apis"
3. Same as MAC OS
4. Hope netflix has a linux client soon 

Windows
=======
1. Same as MAC OS
2. 
3. Same as MAC OS

